<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="csrf-token" content="<%= csrfToken %>">
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="./css/style.css">
    <title><%=title%></title>
    <style>
        #bt{
            border-radius: 50px;
        }
    </style>
</head>
<body>
    <div class="grid grid-cols-6">
        <div class="col-start-3 col-span-2">
            <%- include('header.ejs') %>
            <h6 class="py-5">Sign Up as a new user</h6>
            <div>
                <% if (messages && messages.error && messages.error.length > 0) { %>
                    <% for(var i=0; i<messages.error.length; i++) { %>
                        <p class="bg-red-100 my-3 list-none rounded px-4 py-2"><%= messages.error[i] %></p>
                    <% } %>
                <% } %>
            </div>
            <form style="font-weight: bolder;" action="/users" method="post">
                <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
                <div class="py-4">
                    <label for="firstName">First Name: </label>
                    <input placeholder="Enter Your First Name" id="firstName" type="text" name="firstName" class="border border-color-300 text-gray-900 rounded p-2 w-full">
                </div>
                <div class="py-4">
                    <label for="lastName">Last Name: </label>
                    <input placeholder="Enter Your Last Name" id="lastName" type="text" name="lastName" class="border border-color-300 text-gray-900 rounded p-2 w-full">
                </div>
                <div class="py-4">
                    <label for="email">EmailID : </label>
                    <input placeholder="Enter Your Email Id" id="email" type="email" name="email" class="border border-color-300 text-gray-900 rounded p-2 w-full">
                </div>
                <div class="py-4">
                    <label for="password">Password: </label>
                    <input placeholder="Enter Your Password" id="password" type="password" name="password" class="border border-color-300 text-gray-900 rounded p-2 w-full">
                </div>
                <button type="submit" style="    border: 3px solid black;
                padding: 5px;
                border-radius: 5px;
                margin-left: 23px;    background-color: #9fa9a9;" id="bt">Sign up</button>
            </form>
            
        </div>
    </div>
</body>
</html>